apiVersion: v1
kind: Secret
metadata:
  name: jobability-secrets
  namespace: jobability
type: Opaque
stringData:
  DJANGO_SECRET_KEY: replace-with-long-random-string
  POSTGRES_PASSWORD: replace-with-database-password
  MINIO_ROOT_USER: jobability
  MINIO_ROOT_PASSWORD: replace-with-minio-password
